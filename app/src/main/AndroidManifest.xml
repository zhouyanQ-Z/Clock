<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.swufe.clock">
    <!-- 振动器的权限 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TimerActivity">
        </activity>
        <activity android:name=".SecondTimerActivity"></activity>
        <activity android:name=".TimeListActivity" />
        <activity android:name=".AlarmActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalculatorActivity_play"/>
        <activity android:name=".CalculatorActivity_selected"/>
        <activity android:name=".AlarmAdd" />

        <!-- 注册广播接收器 -->
<!--        <receiver-->
<!--            android:name=".AlarmReceiver"-->
<!--            android:enabled="true"-->
<!--            android:process=":remote">-->
<!--            <intent-filter>-->
<!--                <action android:name="com.swufe.clock" />-->
<!--            </intent-filter>-->
<!--        </receiver> &lt;!&ndash; 注册目的Activity &ndash;&gt;-->
<!--        <activity android:name=".PlayAlarm" />-->

    </application>

</manifest>